on:
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Set up dotnet
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.100'
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build
        run: dotnet build src/DevilDaggersInfo.Core.slnx -c Release
      - name: Test
        run: dotnet test src/DevilDaggersInfo.Core.slnx -c Release --no-build
